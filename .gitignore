# Training data and model weights (too large for git)
data/
models/*.keras
models/*.h5

# Weather words content — pending permission to publish
words.html

# Keep the TF.js converted model (small enough for Pages)
# models/tfjs/ is tracked

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Cloudflare
.wrangler/

# Docker / secrets
.env
docker-compose.override.yml

# Wrangler local secrets (ADMIN_TOKEN etc. for `wrangler dev`)
.dev.vars
.dev.vars.*
!.dev.vars.example

# data/ is a SYMLINK to ~/data/wxwords/data — the `data/` rule above matches
# directories only, so the symlink needs its own slashless entry.
/data

# Temp
*.tmp
*.log

# ─── PERSONAL DATA — NEVER COMMIT (this repo is PUBLIC on GitHub) ───
# Gmail thread exports (full message bodies + personal address)
scripts/threads/
# Flight / loyalty / Notion personal exports
scripts/flight_*.json
scripts/flights_*.json
scripts/loyalty_*.json
scripts/notion_pages.json
scripts/page_data_partial.json
scripts/page_id_map.json
scripts/migration_*.json
scripts/dupe_cleanup_plan.json
scripts/_notion_pages_collected.txt
